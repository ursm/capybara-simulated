# WPT IN-SCOPE backlog — subtests that currently fail but are real driver gaps
# we intend to fix (the conformance roadmap). Earned out-of-scope failures live
# in wpt_out_of_scope.yml. The gate (spec/wpt_spec.rb) loads BOTH and checks the
# union symmetrically: a non-PASS subtest in NEITHER file -> RED; a listed subtest
# that now PASSes -> RED. New failures default here (in-scope) until shown to be
# an earned non-goal, then moved to wpt_out_of_scope.yml with a reason.
#
# Each entry is one file: a list of not-yet-passing subtest names, or the string
# HARNESS_ERROR if the harness never completed. Shrinking THIS file is the roadmap.
#
# Regenerate after a driver fix:  bundle exec ruby script/regen_wpt_expected_failures.rb
#
---
dom/events/Event-dispatch-redispatch.html:
- Redispatching mouseup event whose default action dispatches a click event
html/semantics/forms/constraints/input-maxlength-emoji.html:
- Emoji gets truncated due to maxlength attribute
html/semantics/forms/form-submission-0/action-encoding.html:
- Encoding of form action URL
html/semantics/forms/form-submission-0/constructing-form-data-set.html:
- _charset_ control sets the expected encoding name.
html/semantics/forms/form-submission-0/form-double-submit-multiple-targets.html:
- Verifies that one form used to target multiple frames in succession navigates all
  of them.
html/semantics/forms/form-submission-0/form-submit-iframe-then-location-navigate.html:
- Verifies that location navigations take precedence when following form submissions.
html/semantics/forms/form-submission-0/submit-file.sub.html:
- Posting a File
html/semantics/forms/historical-search-event.html:
- search event should not be supported
html/semantics/forms/input-change-event-properties.html:
- "<select> pressing down arrow"
html/semantics/forms/the-fieldset-element/disabled-003.html:
- Disable deep-nested fieldsets should not hang
- Disable light-nested fieldsets should not crash
html/semantics/forms/the-input-element/input-disabled-fieldset-dynamic.html:
- input-disabled-fieldset-dynamic
html/semantics/forms/the-input-element/radio-focus-navigation-group-focusable-focus.html:
- During arrow key navigation, focus and checked is set on the radio element.
html/semantics/forms/the-input-element/radio-keyboard-navigation-order.html:
- Radio button keyboard navigation should proceed in tree-order.
html/semantics/forms/the-select-element/customizable-select-multiple-popup/selectedcontent-multiple-restore.optional.html:
- "<selectedcontent> should be up to date after form restoration."
html/semantics/forms/the-select-element/customizable-select/nested-options.html:
- Nested <options> not should be listed in <select> IDLs.
html/semantics/forms/the-select-element/customizable-select/option-color-inheritance.html:
- option elements should inherit color from their parent select element.
html/semantics/forms/the-select-element/customizable-select/option-disabled-optgroup.html:
- disabled select
- nesting options and optgroups in disabled select
- options should check ancestor optgroup for disabled state.
html/semantics/forms/the-select-element/customizable-select/option-list.html:
- select's option list should not include descendants of options, hrs, or nested optgroups.
html/semantics/forms/the-select-element/customizable-select/selectedcontent-insertion-removal.html:
- Option element insertion steps.
- Option element removal steps.
html/semantics/forms/the-select-element/customizable-select/selectedcontent-movebefore.html:
- Selectedcontent behavior with moving steps.
html/semantics/forms/the-select-element/customizable-select/selectedcontent-mutations.html:
- 'MutationObserver records during parsing of <select> with <selectedcontent>: test1'
- 'MutationObserver records during parsing of <select> with <selectedcontent>: test2'
- 'MutationObserver records during parsing of <select> with <selectedcontent>: test3'
- 'MutationObserver records during parsing of <select> with <selectedcontent>: test4'
- 'MutationObserver records during parsing of <select> with <selectedcontent>: test5'
- 'MutationObserver records during parsing of <select> with <selectedcontent>: test6'
- 'MutationObserver records during parsing of <select> with <selectedcontent>: test7'
- 'MutationObserver records during parsing of <select> with <selectedcontent>: test8'
html/semantics/forms/the-select-element/customizable-select/selectedcontent-restore.optional.html:
- "<input> inside <selectedcontent> should be restored after form submission."
- "<selectedcontent> should be up to date after form restoration."
html/semantics/forms/the-select-element/select-restore-invalid-option.html:
- The select element should reset to its initial state when restoring a mismatched
  value.
html/semantics/forms/the-textarea-element/wrap-enumerated-ascii-case-insensitive.html:
- keywords
shadow-dom/focus/click-focus-delegatesFocus-click.html:
- click on the host with delegatesFocus with another host with delegatesFocus and
  a focusable child
- click on the host with delegatesFocus with another host with no delegatesFocus and
  a focusable child
- click on the host with no delegatesFocus with another host with delegatesFocus and
  a focusable child
- click on the host with no delegatesFocus with another host with no delegatesFocus
  and a focusable child
