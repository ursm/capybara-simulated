{
  "name": "capybara-simulated-vendor",
  "private": true,
  "description": "Build script that bundles happy-dom into a single browser-compatible JS file for mini_racer.",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "@hotwired/stimulus": "^3.2.2",
    "@hotwired/turbo": "^8.0.23",
    "esbuild": "^0.28.0",
    "fontoxpath": "^3.34.0",
    "happy-dom": "^20.0.0",
    "whatwg-url": "^16.0.1"
  }
}
