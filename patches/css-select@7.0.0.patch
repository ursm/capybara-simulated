diff --git a/dist/attributes.js b/dist/attributes.js
index 89dab0a26c12f18182e4c98b69bde2b68fc7fc2f..de89aa6ed9ae0c2caaf1fa552a6e31b7e06064c0 100644
--- a/dist/attributes.js
+++ b/dist/attributes.js
@@ -67,7 +67,12 @@ function shouldIgnoreCase(selector, options) {
         ? selector.ignoreCase
         : selector.ignoreCase === "quirks"
             ? !!options.quirksMode
-            : !options.xmlMode && caseInsensitiveAttributes.has(selector.name);
+            // csim patch: `lowerCaseAttributeNames: false` keeps `selector.name`
+            // case-preserved (so the adapter folds per element-namespace), but the
+            // HTML "case-insensitive attribute VALUE" list is keyed by the
+            // canonical lowercase name — fold here so `[TYPE=text]` still
+            // value-matches case-insensitively like `[type=text]`.
+            : !options.xmlMode && caseInsensitiveAttributes.has(selector.name.toLowerCase());
 }
 /**
  * Attribute selectors
@@ -117,7 +122,10 @@ export const attributeRules = {
     element(next, data, options) {
         const { adapter } = options;
         const { name, value } = data;
-        if (whitespaceRe.test(value)) {
+        // Per Selectors §6.3.2, `[attr~=val]` with an empty `val` (or a `val`
+        // containing whitespace, which can never be a single token) matches
+        // nothing — same empty-value short-circuit `^=`/`$=`/`*=` already have.
+        if (value === "" || whitespaceRe.test(value)) {
             return boolbase.falseFunc;
         }
         const regex = new RegExp(`(?:^|\\s)${escapeRegex(value)}(?:$|\\s)`, shouldIgnoreCase(data, options) ? "i" : "");
diff --git a/dist/general.js b/dist/general.js
index 5d3fdf164e2a7e9612dec3cf6bdb2768a14d06ed..e2ed9e28d75c5f51857b6e9e567fd27f82a94f13 100644
--- a/dist/general.js
+++ b/dist/general.js
@@ -27,7 +27,13 @@ export function compileGeneralSelector(next, selector, options, context, compile
             if (selector.namespace != null) {
                 throw new Error("Namespaced attributes are not yet supported by css-select");
             }
-            if (!options.xmlMode || options.lowerCaseAttributeNames) {
+            // csim patch: an explicit `lowerCaseAttributeNames: false` keeps the
+            // selector's attribute name CASE-PRESERVED even in HTML mode, so the
+            // adapter can fold case per-element (HTML = case-insensitive, SVG /
+            // MathML = case-sensitive) — a distinction css-select's global option
+            // can't express. Default (option unset) is unchanged.
+            if (options.lowerCaseAttributeNames !== false &&
+                (!options.xmlMode || options.lowerCaseAttributeNames)) {
                 selector.name = selector.name.toLowerCase();
             }
             return attributeRules[selector.action](next, selector, options);
diff --git a/dist/pseudo-selectors/aliases.js b/dist/pseudo-selectors/aliases.js
index 8fb6633f09e40d21d668be06182a3868f0fc8dff..b2c7529cdf1c8b2a9933f4de70fbd076e793202c 100644
--- a/dist/pseudo-selectors/aliases.js
+++ b/dist/pseudo-selectors/aliases.js
@@ -10,7 +10,10 @@ const textControl = "input:is([type=text i],[type=search i],[type=url i],[type=t
  */
 export const aliases = {
     // Links
-    "any-link": ":is(a, area, link)[href]",
+    // csim patch: scope :any-link / :link to <a>/<area> only — browsers (and
+    // the WPT selectors suite) do NOT match <link href> with :link, despite the
+    // bare spec text. Without this `#head :link` wrongly matches a <link href>.
+    "any-link": ":is(a, area)[href]",
     link: ":any-link:not(:visited)",
     // Forms
     // https://html.spec.whatwg.org/multipage/scripting.html#disabled-elements
diff --git a/dist/pseudo-selectors/filters.js b/dist/pseudo-selectors/filters.js
index 241517546e40497c1fa6005de6d779b97fba0d61..686db0da3e4cf5b86ed859bc39c57a0f57db58e4 100644
--- a/dist/pseudo-selectors/filters.js
+++ b/dist/pseudo-selectors/filters.js
@@ -98,9 +98,13 @@ export const filters = {
     "nth-last-child": compileNth(true, false),
     "nth-of-type": compileNth(false, true),
     "nth-last-of-type": compileNth(true, true),
-    // TODO determine the actual root element
+    // csim patch: `:root` matches ONLY the document's root element. The default
+    // "no element parent" test wrongly matches a DocumentFragment's top child;
+    // defer to `adapter.isDocumentRoot` when provided.
     root(next, _rule, { adapter }) {
-        return (element) => getElementParent(element, adapter) === null && next(element);
+        return (element) => (typeof adapter.isDocumentRoot === "function"
+            ? adapter.isDocumentRoot(element)
+            : getElementParent(element, adapter) === null) && next(element);
     },
     scope(next, rule, options, context) {
         const { equals } = options;
diff --git a/dist/pseudo-selectors/pseudos.js b/dist/pseudo-selectors/pseudos.js
index 0790e2aaa067b1e1bdc13724838185d2c011083c..88e6c628fc46f66864c9bd86d8023f30bfede1f4 100644
--- a/dist/pseudo-selectors/pseudos.js
+++ b/dist/pseudo-selectors/pseudos.js
@@ -1,22 +1,15 @@
-/**
- * CSS limits the characters considered as whitespace to space, tab & line
- * feed. We add carriage returns as htmlparser2 doesn't normalize them to
- * line feeds.
- * @see {@link https://www.w3.org/TR/css-text-3/#white-space}
- */
-const isDocumentWhiteSpace = /^[ \t\r\n]*$/;
 // While filters are precompiled, pseudos get called when they are needed
 /** Runtime pseudo selector implementations. */
 export const pseudos = {
     empty(element, { adapter }) {
         const children = adapter.getChildren(element);
-        return (
-        // First, make sure the tag does not have any element children.
-        children.every((element) => !adapter.isTag(element)) &&
-            // Then, check that the text content is only whitespace.
-            children.every((element) => 
-            // FIXME: `getText` call is potentially expensive.
-            isDocumentWhiteSpace.test(adapter.getText(element))));
+        // Per Selectors §:empty, an element is :empty iff it has no element
+        // children and no child text node with data — comments and PIs (whose
+        // `getText` is "") don't count, but a whitespace-only text node DOES
+        // (every browser disqualifies `<p> </p>`). The previous test treated
+        // whitespace-only text as empty, which no browser does. Short-circuiting
+        // on `isTag` also skips the FIXME'd `getText` call for element children.
+        return children.every((child) => !adapter.isTag(child) && adapter.getText(child) === "");
     },
     "first-child"(element, { adapter, equals }) {
         if (adapter.prevElementSibling) {
